---
rules:
  unpinned-uses:
    config:
      policies:
        # Allow same-org internal reusable workflows referenced by branch (e.g. @main)
        ut-issl/*: ref-pin
  ref-confusion:
    ignore:
      - auto-label-subsystem.yml
      - close-set-iteration.yml
